// Bare test page — confirms the deploy chain renders a fresh route.
export default function TestPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <h1 className="text-4xl font-semibold">It works.</h1>
    </main>
  );
}
